import React, { useContext, useEffect, useState, lazy } from 'react';

import Box from '@mui/material/Box';

const Main = () => {

    return (
        <Box sx={{ display: 'flex' }}>
            <> test </>
        </Box>
    );
};

export default Main;