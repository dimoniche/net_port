import React, { useContext, useEffect, useState, lazy } from 'react';

import Box from '@mui/material/Box';

const Main = () => {

    return (
        <Box sx={{ display: 'flex' }}>
            <> Программа проксирования портов без выделения IP адреса </>
        </Box>
    );
};

export default Main;